{
  "name": "cko-customer-lookup",
  "version": "1.0.0",
  "description": "Slack slash command to check if a company is a Checkout.com customer via Salesforce",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
